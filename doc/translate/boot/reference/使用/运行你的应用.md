# Running Your Application

将应用程序打包为 jar 文件并使用嵌入式 HTTP 服务器的一个最大优势在于，你可以像运行其他任何应用程序一样运行它。调试 Spring Boot 应用程序也是如此。你不需要任何特殊的 IDE 插件或扩展。

以下选项最适合在本地运行应用程序以进行开发。有关生产环境部署，请参阅“为生产环境打包应用程序”。

本节仅涵盖基于 jar 的打包方式。如果你选择将应用程序打包为 war 文件，请参阅你的服务器和 IDE 文档。

从 IDE 运行
您可以从 IDE 将 Spring Boot 应用程序作为 Java 应用程序运行。但是，首先需要导入您的项目。导入步骤因 IDE 和构建工具的不同而有所差异。大多数 IDE 可以直接导入 Maven 项目。例如，Eclipse 用户可以从文件菜单中选择“导入… → 现有 Maven 项目”。

如果无法直接将项目导入 IDE，您可以通过使用构建工具插件生成 IDE 元数据。Maven 提供了适用于 Eclipse 和 IntelliJ IDEA 的插件。Gradle 也提供了适用于各种 IDE 的插件。

如果您不小心两次运行了 Web 应用程序，会看到“端口已被占用”的错误。Spring Tools 用户可以使用“重新启动”按钮，而不是“运行”按钮，以确保关闭任何现有的实例。


作为打包应用程序运行 
如果您使用 Spring Boot 的 Maven 或 Gradle 插件来创建可执行的 jar 文件，可以使用 `java -jar` 命令运行您的应用程序，如下例所示：

```java

$ java -jar target/myapplication-0.0.1-SNAPSHOT.jar
```

也可以运行启用了远程调试支持的打包应用程序。这样做可以让你将调试器附加到你的打包应用程序上，如下例所示：

```java

$ java -agentlib:jdwp=server=y,transport=dt_socket,address=8000,suspend=n \
       -jar target/myapplication-0.0.1-SNAPSHOT.jar
```

使用 Maven 插件
Spring Boot Maven 插件包含一个运行目标（run goal），可用于快速编译并运行您的应用程序。应用程序以解压形式运行，就像在 IDE 中一样。以下示例展示了运行 Spring Boot 应用程序的典型 Maven 命令：
```
$ mvn spring-boot:run
```
您可能还希望使用 MAVEN_OPTS 操作系统环境变量，如下例所示：
```
$ export MAVEN_OPTS=-Xmx1024m
```

使用 Gradle 插件
Spring Boot Gradle 插件同样包含一个 `bootRun` 任务，可用于以解压形式运行应用程序。每当应用了 `org.springframework.boot` 和 `java` 插件时，`bootRun` 任务就会被添加，如下例所示：
```
$ gradle bootRun
```
你可能还想使用 `JAVA_OPTS` 操作系统环境变量，如下例所示：
```
$ export JAVA_OPTS=-Xmx1024m
```
热部署
由于 Spring Boot 应用程序是普通的 Java 应用程序，JVM 的热部署功能应该可以开箱即用。不过，JVM 热部署在可以替换的字节码方面存在一定的限制。如果需要更完整的解决方案，可以使用 JRebel。
`spring-boot-devtools` 模块还支持快速重启应用程序。详情请参见“使用指南”中的“热部署”部分。


# 参考文档
- [jrebel](https://www.jrebel.com/products/jrebel)
- [Hot Swapping](https://docs.spring.io/spring-boot/how-to/hotswapping.html)
- [Running Your Application](https://docs.spring.io/spring-boot/reference/using/running-your-application.html)